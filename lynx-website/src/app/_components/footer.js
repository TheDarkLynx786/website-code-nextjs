import styles from "./footer.module.css"

export default function Footer() {
    return(
        <footer className={styles.footer}>
            <div className={styles.div}>
                <p className={styles.divText}>People be doing copyrights now??</p>
            </div>
        </footer>
    );
}